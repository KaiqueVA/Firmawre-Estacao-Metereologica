{
  "name": "ESP-IDF v5.5 (compose)",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "idf",
  "workspaceFolder": "/work",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "espressif.esp-idf-extension"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'grep -q export.sh ~/.bashrc || echo \". $IDF_PATH/export.sh\" >> ~/.bashrc'",
  "postStartCommand": "bash -lc 'export LANG=C.UTF-8 LC_ALL=C.UTF-8; grep -q LANG ~/.bashrc || echo \"export LANG=C.UTF-8\" >> ~/.bashrc; grep -q LC_ALL ~/.bashrc || echo \"export LC_ALL=C.UTF-8\" >> ~/.bashrc; . $IDF_PATH/export.sh || true'"
  
  
}